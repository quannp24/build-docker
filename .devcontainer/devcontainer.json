{
  "name": "Python 3.4 Project",
  "build": {
    "dockerfile": "Dockerfile"
  },

  // Cài đặt sẵn extension Python cho VS Code
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },

  // Chạy lệnh này sau khi môi trường được tạo để kiểm tra
  "postCreateCommand": "python3 --version"
}
